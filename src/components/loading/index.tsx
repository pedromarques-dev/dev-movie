import { LoaderContainer } from "./styles"

export const Loading = () => {
    return (
        <LoaderContainer>
            <img src="./loading.svg" alt="Loading"/>
        </LoaderContainer>
    )
}